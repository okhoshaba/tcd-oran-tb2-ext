# Kubernetes Mode

This directory contains starter Kubernetes manifests and overlays for Tb2-ext.

- `base/` contains common placeholder resources.
- `overlays/` contains local and group-specific Kustomize overlays.
- The manifests are educational templates until validated in a target cluster.
