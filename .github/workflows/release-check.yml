name: release-check

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check repository metadata
        run: |
          test -f README.md
          test -f LICENSE
          test -f CITATION.cff
