# Compose Mode

The Compose assets in this directory provide a safe Tb2-ext scaffold for local experimentation.

- The current file is syntactically valid but placeholder-based.
- It does not claim to reproduce a full Tb2 runtime.
- It is intended to be extended later with real Tb2-derived services.
