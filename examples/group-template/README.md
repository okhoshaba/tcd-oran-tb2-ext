# Group Template Example

This example directory shows how a student group can document its work around a derived Kubernetes overlay and lab outputs.

Recommended additions for a real group instance:

- group-specific overlay notes;
- validation evidence;
- short deployment diary;
- limitations encountered.
- confirmation that no secrets or private cluster details were committed.
